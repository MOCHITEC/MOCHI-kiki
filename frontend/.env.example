# /api/console/* を実バックエンド (src/main.py の aiohttp) にプロキシしたい時だけ設定
# - 未設定なら frontend 内の dev mock (src/app/api/console/*) が応答する (admin/admin で入れる)
# - 設定する場合は backend の PORT 既定値と揃える (3978)
# BACKEND_URL=http://localhost:3978
